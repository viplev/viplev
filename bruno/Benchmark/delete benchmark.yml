info:
  name: /v1/environments/:id/benchmarks/:id - Delete a benchmark
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{http_url}}/v1/environments/:environmentId/benchmarks/:benchmarkId"
  params:
    - name: environmentId
      value: "{{environment_id}}"
      type: path
    - name: benchmarkId
      value: "{{benchmark_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
