info:
  name: /v1/environments/:id/message - Get pending messages
  type: http
  seq: 2

http:
  method: GET
  url: "{{http_url}}/v1/environments/:environmentId/message"
  params:
    - name: environmentId
      value: "{{environment_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
