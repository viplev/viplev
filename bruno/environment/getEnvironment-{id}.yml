info:
  name: getEnvironment/{id}
  type: http
  seq: 3

http:
  method: GET
  url: "{{http_url}}/v1/environments/{{environment_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
