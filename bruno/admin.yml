info:
  name: /admin
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/secured
  body:
    type: json
    data: |-
      {
        "username": "user1@viplev.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
