info:
  name: /auth/login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/v1/auth/login
  body:
    type: json
    data: |-
      {
        "username": "user1@viplev.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
